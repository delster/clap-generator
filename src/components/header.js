import React from 'react'
import styled from 'styled-components'

const StyledHeader = styled.header`
  text-align: center;
`

const Header = () => (
  <StyledHeader>
    <h1>CLAP 👏 TEXT 👏 GENERATOR</h1>
  </StyledHeader>
)

export default Header
